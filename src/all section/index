<div b-kksllyi9sk="" class="progressBar-wrapper mt-2" style="translate: none; rotate: none; scale: none; opacity: 1; visibility: inherit; transform: translate(0px, 0px);">
        <div b-kksllyi9sk="" class="percent">
            <svg b-kksllyi9sk="">
                <circle b-kksllyi9sk="" cx="25" cy="25" r="20"></circle>
                <circle b-kksllyi9sk="" cx="25" cy="25" r="20" class="svg-progress" style="stroke-dashoffset: 86px; stroke-dasharray: 125;"></circle>
            </svg>
            <span b-kksllyi9sk="">↑</span>
        </div>
    </div>


    translate: none;
    rotate: none;
    scale: none;
    opacity: 1;
    visibility: inherit;
    transform: translate(0px, 0px);
}
.progressBar-wrapper {
    z-index: 9999;
    position: fixed;
    bottom: 20px;
    right: 20px;
    color: var(--secondary-color);
    background-color: var(--white-color);
    width: 50px;
    height: 50px;
    border-radius: 50%;
    display: grid;
    place-items: center;
    cursor: pointer;
}

translate: none;
    rotate: none;
    scale: none;
    opacity: 1;
    visibility: inherit;
    transform: translate(0px, 0px);
}
.progressBar-wrapper {
    z-index: 9999;
    position: fixed;
    bottom: 20px;
    right: 20px;
    color: var(--secondary-color);
    background-color: var(--white-color);
    width: 50px;
    height: 50px;
    border-radius: 50%;
    display: grid;
    place-items: center;
    cursor: pointer;
}

translate: none;
    rotate: none;
    scale: none;
    opacity: 1;
    visibility: inherit;
    transform: translate(0px, 0px);
}
.progressBar-wrapper {
    z-index: 9999;
    position: fixed;
    bottom: 20px;
    right: 20px;
    color: var(--secondary-color);
    background-color: var(--white-color);
    width: 50px;
    height: 50px;
    border-radius: 50%;
    display: grid;
    place-items: center;
    cursor: pointer;
}

.progressBar-wrapper .percent svg circle {
    fill: none;
    stroke: var(--secondary-color);
    stroke: #ddd;
    stroke-width: 5;
    stroke-linecap: round;
    height: 100%;
    width: 100%;
}
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}
*, ::after, ::before {
    box-sizing: border-box;
}
circle[Attributes Style] {
    cx: 25;
    cy: 25;
    r: 20;
}
user agent stylesheet
:not(svg) {
    transform-origin: 0px 0px;
}
style attribute {
    translate: none;
    rotate: none;
    scale: none;
    opacity: 1;
    visibility: inherit;
    transform: translate(0px, 0px);
}
.progressBar-wrapper {
    z-index: 9999;
    position: fixed;
    bottom: 20px;
    right: 20px;
    color: var(--secondary-color);
    background-color: var(--white-color);
    width: 50px;
    height: 50px;
    border-radius: 50%;
    display: grid;
    place-items: center;
    cursor: pointer;
}
body {
    font-family: "Media Sans";
    height: 100vh;
    width: 100vw;
    overflow-x: hidden;
}
body {
    margin: 0;
    font-family: var(--bs-body-font-family);
    font-size: var(--bs-body-font-size);
    font-weight: var(--bs-body-font-weight);
    line-height: var(--bs-body-line-height);
    color: var(--bs-body-color);
    text-align: var(--bs-body-text-align);
    background-color: var(--bs-body-bg);
    -webkit-text-size-adjust: 100%;
    -webkit-tap-highlight-color: transparent;
}

stroke-dashoffset: 86px;
stroke-dasharray: 125;
}
.progressBar-wrapper .percent svg .svg-progress {
stroke: var(--secondary-color);
transition: all 0.1s linear;
}
.progressBar-wrapper .percent svg circle {
fill: none;
stroke: var(--secondary-color);
stroke: #ddd;
stroke-width: 5;
stroke-linecap: round;
height: 100%;
width: 100%;
}

.progressBar-wrapper .percent span {
    left: 50%;
    position: absolute;
    top: 50%;
    -webkit-transform: translate(-50%, -50%);
    transform: translate(-50%, -50%);
}
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}
*, ::after, ::before {
    box-sizing: border-box;
}